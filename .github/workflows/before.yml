# 修改hostname
# test 3
# on push 
# add code review
# review test 
name: GitHub Actions before 

on:
  pull_request:
    branches:
      - master
  # push:
  #   branches:
  #     - master

jobs:
  myfirstjob:
    name: My Job
    runs-on: ubuntu-latest
    steps:
    - name: Print a greeting
      run: |
            echo "Welcome" 

